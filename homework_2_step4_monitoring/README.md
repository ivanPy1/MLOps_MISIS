# Homework 2 Step 4: Мониторинг FastAPI сервиса

## Описание

Реализована система мониторинга для FastAPI сервиса из шага 2 с настраиваемыми алертами и цветным логированием.

## Структура

```
Конечно, вот структура вашего проекта в формате Markdown, отражающая все изменения и новые модули для мониторинга:

# 📁 Структура проекта

```.
step4_monitoring/
├── config/
│   └── monitoring_config.yaml  # Конфигурация порогов, интервалов и адресов для мониторинга.
|
├── logs/                       # Папка для всех логов (создается автоматически).
│   ├── monitoring.log          # Основной структурированный лог в формате JSON
│   └── metrics.jsonl           # Лог вычислительных метрик в формате JSON Lines
|
├── models/
│   └── blip_model.onnx       # Файл ONNX модели генерации описаний.
|
├── src/
│   ├── __init__.py
│   ├── api.py                  # FastAPI приложение с эндпоинтами /health и /predict.
│   ├── model_service.py        # Сервис загрузки и инференса ONNX модели.
│   ├── config.py               # Модуль для загрузки и доступа к monitoring_config.yaml.
│   ├── logger.py               # Модуль для цветного консольного и структурированного JSON логирования
│   └── monitor.py              # Основная логика мониторинга, проверки порогов и системы алертов
|
├── test_images/                # Папка с тестовыми изображениями
|
├── main.py                     # Главный скрипт: запускает FastAPI (uvicorn) и сервис мониторинга параллельно
├── client_test.py              # Тестовый асинхронный клиент для проверки API
└── pyproject.toml              # Файл зависимостей Poetry
```

## Установка

```bash
poetry install
poetry shell
```

## Подготовка

Скопируйте ONNX модель из step1:
```bash
cp ../step1_onnx_model/models/blip_model.onnx models/
```

## Запуск сервиса

```bash
python main.py
```

Сервис будет доступен по адресам:
- API: http://localhost:8000
- Документация: http://localhost:8000/docs
- Health check: http://localhost:8000/health

## Тестирование

В отдельном терминале:
```bash
python client_test.py
```

## API Endpoints

### GET /health
Проверка состояния сервиса

### POST /predict
Инференс одного изображения
- Принимает: multipart/form-data с изображением
- Возвращает: результат инференса с метриками времени

### POST /predict_batch
Batch инференс нескольких изображений
- Принимает: multipart/form-data с несколькими изображениями (до 10)
- Возвращает: результаты всех изображений + статистика батча

### GET /metrics
Метрики сервиса

## Особенности

### 1. Итегрирован с FastAPI сервисом
- Использовать сервис инференса ONNX модели 
- FastAPI сервис запускается программно
- Мониторинг endpoints: `/health` и `/predict`

### 2. Мониторинг основных метрик
- **Response Time**: время отклика API запросов
- **P95 Latency**: 95-й перцентиль времени ответа
- **Error Rate**: процент неудачных запросов
- **Health Status**: статус работоспособности сервиса
- **Consecutive Failures**: количество последовательных ошибок

### 3. Тестирование инференса
- Позволяет отправлять POST запросы с изображениями на `/predict`
- Поддерживает любые форматы изображений (jpg, png)
- Логирует время обработки и результат предсказания
- Проверяет корректность ответа API

### 4. Цветные алерты
Реализована система алертов с цветовой индикацией:

- **Зеленый**: Нормальная работа (метрики в пределах нормы)
- **Желтый**: Предупреждение (превышены warning пороги)
- **Красный**: Критическое состояние (превышены critical пороги)

### 5. Использует конфигурируемые пороговые значения в `monitoring_config.yaml`
